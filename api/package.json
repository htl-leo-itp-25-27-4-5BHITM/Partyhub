{
  "name": "api",
  "version": "1.0.0",
  "description": "PartyHub API Integration Tests",
  "main": "index.js",
  "scripts": {
    "test": "httpyac --all --timeout 10000 --verbose *.http",
    "test:ci": "httpyac --all --timeout 10000 --verbose --output ./test-results/ *.http"
  },
  "keywords": [
    "httpyac",
    "api",
    "integration-tests"
  ],
  "author": "PartyHub",
  "license": "ISC",
  "dependencies": {
    "httpyac": "^6.16.7"
  }
}
